import React from 'react'
import Popup from '../Popup'

const CustomersEdit = (props) => {
    return (
        <div>
            <Popup>
                
            </Popup>
        </div>
    )
}

export default CustomersEdit
